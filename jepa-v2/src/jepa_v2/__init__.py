"""jepa-v2: factored self-supervised program embedding on ProgramML graphs.

z = [z_sem | z_speed]
  z_sem   — invariant to the -O level (what the code does)
  z_speed — invariant to the program (the optimization / speed profile)
"""
__version__ = "0.1.0"
